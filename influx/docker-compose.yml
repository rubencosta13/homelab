services:
  influxdb:
    image: influxdb
    container_name: influxdb
    restart: unless-stopped
    ports:
      - "${INFLUXDB_HTTP_PORT}:8086"
    env_file:
      - .env
    volumes:
      - influxdb_data:/var/lib/influxdb2

volumes:
  influxdb_data: